# laravel-cloud-scale-zero-demo
This example demonstrates a simple PHP web application endpoint, similar to what would run on Laravel Cloud. It simulates a small amount of processing time for each request. The example highlights how such an application benefits from Laravel Cloud's managed hosting, cost-efficiency, and 'instant scale to zero' feature, where resources are consumed
